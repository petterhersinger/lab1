@model List<PlayerTeamViewModel>

<h1>Spelarregister</h1>

<table class="table">
    <thead>
        <tr>
            <th>Spelarnamn</th>
            <th>Startar</th>
            <th>Position</th>
            <th>Lag</th>
            <th>Ta bort</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var player in Model)
        {
            <tr>
                <td>@player.Player.Name</td>
                <td>@player.Player.IsStarter ? "Ja" : "Nej")</td>
                <td>@player.Player.Position</td>
                <td>@player.Team.TeamName</td>
                <td>
                    <form method="post" asp-action="RemovePlayer">
                        <input type="hidden" name="playerId" value="@player.Player.Id"/>
                        <button type="submit" class="btn btn-danger">Ta bort</button>
                    </form>
                </td>
            </tr>
        }
    </tbody>
</table>

